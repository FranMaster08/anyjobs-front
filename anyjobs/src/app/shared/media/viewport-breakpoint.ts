/** Umbral mobile/desktop alineado con shell y reels-feed (≤900px = mobile). */
export const VIEWPORT_MOBILE_MAX_PX = 900;

export const VIEWPORT_DESKTOP_MIN_MQ = `(min-width: ${VIEWPORT_MOBILE_MAX_PX + 1}px)`;
